#!/bin/sh

cd `dirname $0`/../..

if [ ! -d "transit-dart" ]; then
  git clone git@github.com:wevre/transit-dart2.git transit-dart
  cd transit-dart
  mkdir -p resources/emits
  dart compile exe -o resources/emits/roundtrip.exe test/roundtrip.dart
else
  echo "transit-dart exists"
fi
